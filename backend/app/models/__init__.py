"""
Model training/inference code lands here in Phase 3 (CatBoost -> LightGBM ->
XGBoost baselines, per DECISIONS.md ADR-008). Empty placeholder for now so
the modular pipeline layout (ingestion -> features -> models -> serving) is
visible in the repo from Phase 2 onward.
"""
