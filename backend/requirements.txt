# Phase 1+2+3 dependencies.
# Later phases will add: mlflow, kafka-python, etc.
# Keeping requirements lean per-phase makes installs fast and failures easy to diagnose.
# (Notebook/plotting-only deps live in ml/requirements.txt, not here — the
# serving image never needs matplotlib/jupyter.)

fastapi==0.115.0
uvicorn[standard]==0.30.6
pandas==2.2.2
pyarrow==17.0.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
redis==5.0.8
pytest==8.3.3

# Feature engineering (Phase 2) — runtime deps for backend/app/features/
h3==4.5.0
pygeohash==2.0.0

# Modeling (Phase 3) — runtime deps for backend/app/models/
catboost==1.2.10
lightgbm==4.6.0
xgboost==3.2.0
scikit-learn==1.9.0
shap==0.52.0

# Risk/recommendation engine (Phase 5) — runtime deps for backend/app/models/
pyyaml==6.0.3
