# Copy to .env.local for local dev. NEXT_PUBLIC_ prefix is required for
# Next.js to expose this to the browser bundle (it's a public API base URL,
# not a secret).

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
