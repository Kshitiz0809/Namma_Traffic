# Data-science / notebook dependencies (EDA, feature validation, modeling
# experiments). Kept separate from backend/requirements.txt so the FastAPI
# serving image stays lean — it never needs matplotlib/jupyter.
# h3/pygeohash are NOT here — they're runtime deps of backend/app/features/,
# so they live in backend/requirements.txt instead.

matplotlib==3.9.2
seaborn==0.13.2
jupyter==1.1.1
nbconvert==7.16.4
nbformat==5.10.4
ipykernel==6.29.5

# Modeling deps land here in Phase 3 (catboost, lightgbm, xgboost, shap) —
# not installed yet, added when that phase actually starts.
